## 1. Econometric-verified schema

- [x] 1.1 Run `openspec schema fork spec-driven-verified econometric-verified` from the repo root (writes to project-local `openspec/schemas/econometric-verified/`, no global install) and verify the forked `schema.yaml` and `templates/` directory exist
- [x] 1.2 Edit `schema.yaml`: rename `design` artifact to `identification` (econometrics-specific instruction: identification strategy, required diagnostics, expected error-structure requirements; `requires: [proposal]`), add `package-selection` artifact (`requires: [identification]`), add `implementation-notes` artifact (`requires: [package-selection, tasks]`), update `verification` artifact to `requires: [implementation-notes]`, add `report` artifact (`requires: [verification]`) and verify `openspec schema validate econometric-verified` passes
- [x] 1.3 Write per-artifact `instruction` text and `templates/*.md` for `identification`, `package-selection`, `implementation-notes`, and `report` (reusing `verification.md`'s template/instruction from the forked `spec-driven-verified` unchanged) and verify `openspec schema validate econometric-verified` still passes
- [x] 1.4 Create a scratch test change using `--schema econometric-verified` and verify `openspec status --change <name> --json` shows the full gated chain (`identification` blocked until `proposal`, `package-selection` blocked until `identification`, `implementation-notes` blocked until `package-selection`+`tasks`, `verification` blocked until `implementation-notes`, `report` blocked until `verification`) matching `specs/econometric-team/schema/spec.md`, then delete the scratch change
- [x] 1.5 Verify `openspec schema validate spec-driven-verified` still passes unchanged and `openspec/config.yaml`'s default schema is untouched

## 2. Subagent definitions

- [x] 2.1 Create `.claude/agents/econ-ceo.md` (opus; tools: Read, Grep, Glob, Bash, Agent) with routing-loop instructions per design.md's "CEO routing loop" and verify the file has valid frontmatter (no prior `.claude/agents/*.md` convention existed in this repo - used Claude Code's standard subagent frontmatter format instead)
- [x] 2.2 Create `.claude/agents/econ-design.md` (opus; tools: Read, Grep, Glob, WebSearch, Write) with instructions to produce the `identification` artifact and enforce the gate, and verify frontmatter is valid
- [x] 2.3 Create `.claude/agents/econ-package-scout.md` (sonnet; tools: Read, WebSearch, Bash, Write) with instructions to produce the `package-selection` artifact (Python vs R comparison with rationale) and verify frontmatter is valid
- [x] 2.4 Create `.claude/agents/econ-estimation.md` (sonnet; tools: Read, Write, Edit, Bash) with instructions to implement the model and produce `implementation-notes` mapping code to the identification spec, and verify frontmatter is valid
- [x] 2.5 Create `.claude/agents/econ-test-writer.md` (sonnet; tools: Read, Write, Edit, Bash) with instructions to write and run diagnostic tests from the identification artifact's required-diagnostics list, and verify frontmatter is valid
- [x] 2.6 Create `.claude/agents/econ-triage.md` (opus; tools: Read, Grep, Bash, Write) with instructions implementing the CODE_BUG/SPEC_GAP/DESIGN_FLAW classification and producing the `verification` artifact once resolved, and verify frontmatter is valid
- [x] 2.7 Create `.claude/agents/econ-writer.md` (sonnet; tools: Read, Write) with instructions to produce the `report` artifact, and verify frontmatter is valid

## 3. Failure-loop and gate verification

- [x] 3.1 Verified live (not a scripted harness - direct subagent invocations, since the scenario needs real judgment, not fixture matching): (a) crafted a CODE_BUG scenario (identification.md + implementation-notes.md claiming clustered SEs + estimate.py that doesn't apply them + failing heteroskedasticity-test output) and invoked `econ-triage` directly - correctly classified CODE_BUG citing the specific missing `cov_type` argument and even flagged a stale line-number reference and an unreported diagnostic; (b) crafted a DESIGN_FLAW scenario (DiD identification.md + code that correctly matches implementation-notes.md + a failing pre-trend/parallel-trends test) and invoked `econ-triage` directly - correctly classified DESIGN_FLAW, correctly ruled out CODE_BUG (code matches notes) and SPEC_GAP (the diagnostic that failed was the one the spec explicitly required and it fired as intended); (c) told `econ-ceo` (hypothetically, no real specialists invoked) that estimation had failed and been retried 3 times, then reported a 4th failure - it correctly refused to route a 4th time, cited its own retry-cap rule verbatim, and reported the failure history for a user decision instead
- [x] 3.2 Verified live: created a scratch econometric-verified change with only `proposal.md` (no `identification`), told `econ-ceo` to skip straight to package selection "because I'm in a hurry" - it refused, cited `openspec status`'s `blocked`/`missingDeps` output and its own gate rule, explained why (package selection needs identification's criteria first), and invoked no specialists without explicit override. Scratch change deleted after the test.

## 4. Documentation

- [x] 4.1 Add a short section to this repo's root-level agent guidance (or create one if none exists) describing the econ-* team, its entry point (`econ-ceo`), and pointing at the `econometric-verified` schema, and verify the reference resolves to real files
